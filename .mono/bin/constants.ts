import monoMDLintConf from '../../.markdownlint.jsonc'
import monoBiomeConf from '../../biome.jsonc'
import monoPJSON from '../../package.json'
import { rootGitBranchName } from './git'
import { resolveRootPath } from './utils/fs'
import { breakTextToLines } from './utils/misc'

export const CLI_INDENT_WIDTH = 2

export const LICENSES = ['agpl-v3', 'cc-by-sa-30', 'mit', 'mpl-2.0'] as const

export const MONO_GIT_URI_HTTPS_REGEX =
    /^https:\/\/(?:(?<user>[^/:]+)@)?(?<server>[^/:@]+)(?::(?<port>\d+))?\/(?<owner>[^/]+)\/(?<repo>[^/]+)\.git$/

export const MONO_GIT_URI_SSH_REGEX =
    /^ssh:\/\/(?<user>[^/:]+)@(?<server>[^/:@]+)(?::(?<port>\d+))?\/(?<owner>[^/]+)\/(?<repo>[^/]+)\.git$/

export const MONO_GIT_URI_SCP_REGEX =
    /^(?<user>[^/:]+)@(?<server>[^/:@]+):(?:(?<port>\d+)\/|\/)?(?<owner>[^/]+)\/(?<repo>[^/]+)\.git$/

export const ENV_NAME_REGEX = /^[A-Z0-9_]+$/
export const ENV_DEFAULT_LINE_WIDTH = 80

export const MONO_HASHTAG_BAR = '#'.repeat(ENV_DEFAULT_LINE_WIDTH - 1)
export const MONO_AUTOGEN_DISCLAIMER = breakTextToLines(
    `This file is auto-generated by Mono. You may edit it, but your changes ` +
        `could be overwritten. Check the documentation for more information.`,
    ENV_DEFAULT_LINE_WIDTH - 3
)
    .map(line => `# ${line}`)
    .join('\n')

export const monoSetupPath = resolveRootPath('.mono.ts')
export const monoEnvPath = resolveRootPath('.mono.env.ts')

export const rootEnvFile = Bun.file(resolveRootPath('.env'))
export const rootEnvExampleFile = Bun.file(resolveRootPath('.env.example'))
export const rootEnvProductionFile = Bun.file(resolveRootPath('.env.production'))

export const ENTRY_ID_REGEX = /^[a-z0-9]+[a-z0-9-]+$/

export const FORMATTED_PERSON_TEXT_REGEX =
    /^(?<name>[^<]+?)\s*(?:<(?<email>[^@>]+@[^>]+)>)?\s*(?:\((?<url>[^)]+)\))?$/

// biome is not a dev dependency at the root
export const monoBiomeVersion = monoPJSON.dependencies['@biomejs/biome']
export { monoBiomeConf }

// markdownlint is not a dev dependency at the root
export const monoMDLintVersion = monoPJSON.dependencies.markdownlint
export { monoMDLintConf }

export const monoAddonGitignoreFile = Bun.file(resolveRootPath('static/addons/.gitignore'))
export const monoAddonEditorConfigFile = Bun.file(resolveRootPath('static/addons/.editorconfig'))

export const ROOT_GIT_BRANCH_NAME = await rootGitBranchName()
